<% content_for :title, "Boards" %>

<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="w-full max-w-5xl mx-auto p-5">
    <!-- APIトークン表示エリア -->
    <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 mb-8 rounded shadow-sm">
      <div class="flex">
        <div class="ml-3">
          <p class="text-sm text-yellow-700">
            <span class="font-bold">拡張機能用APIトークン:</span>
            <code class="bg-yellow-100 px-2 py-1 rounded select-all font-mono text-yellow-800"><%= current_user.authentication_token %></code>
          </p>
          <p class="text-xs text-yellow-600 mt-1">※この文字列をコピーして、Chrome拡張機能の設定画面に入力してください。</p>
        </div>
      </div>
    </div>

    <div class="flex justify-between items-center mb-8">
      <h1 class="font-bold text-3xl text-gray-800">My Boards</h1>
      <%= link_to "＋ Create New Board", new_board_path, class: "rounded-lg py-3 px-6 bg-blue-600 hover:bg-blue-700 text-white font-medium shadow transition" %>
    </div>

    <!-- ボード一覧グリッド -->
    <div id="boards" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @boards.each do |board| %>
        <div class="bg-white rounded-xl shadow border border-gray-100 overflow-hidden hover:shadow-lg transition-shadow duration-300 flex flex-col h-48 group">
          <!-- ボードへのリンクエリア -->
          <!-- 
            ★重要: 本来はボードごとのリンクを表示するためにパラメータを渡すべきですが、
            まずは一旦全リンクが表示される links_path へ遷移するようにしています。
            後ほど「ボードごとの絞り込み」を実装します。
          -->
          <%= link_to links_path(board_id: board.id), class: "flex-grow p-6 bg-gradient-to-br from-blue-50 to-white group-hover:from-blue-100 transition-colors" do %>
            <div class="flex items-start justify-between">
              <h2 class="font-bold text-xl text-gray-800 truncate mb-2"><%= board.name %></h2>
              <span class="bg-blue-100 text-blue-800 text-xs px-2 py-1 rounded-full font-mono">ID: <%= board.id %></span>
            </div>
            <p class="text-gray-500 text-sm mt-2">  
              <%= board.links.count %> cards
            </p>
            <div class="mt-4 text-blue-600 font-medium text-sm flex items-center">
              Open Canvas <span class="ml-1 transition-transform group-hover:translate-x-1">→</span>
            </div>
          <% end %>

          <!-- アクションボタンエリア -->
          <div class="border-t border-gray-100 bg-gray-50 px-4 py-3 flex justify-end gap-3 text-sm">
            <%= link_to "Edit Name", edit_board_path(board), class: "text-gray-500 hover:text-blue-600 font-medium transition-colors" %>
            <span class="text-gray-300">|</span>
            <%= link_to "Delete", board, 
                data: { turbo_method: :delete, turbo_confirm: "「#{board.name}」と、中に含まれる全てのリンクを削除しますか？" }, 
                class: "text-gray-400 hover:text-red-500 transition-colors" 
            %>
          </div>
        </div>
      <% end %>

      <!-- 空の状態 -->
      <% if @boards.empty? %>
        <div class="col-span-full py-12 text-center bg-gray-50 rounded-xl border-2 border-dashed border-gray-200">
          <p class="text-gray-500 mb-4">ボードがまだありません</p>
          <%= link_to "最初のボードを作成する", new_board_path, class: "text-blue-600 font-medium hover:underline" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
